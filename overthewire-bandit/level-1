# The password for the next level is stored in a file called readme located in the home directory. Use this 
# password to log into bandit1 using SSH. Whenever you find a password for a level, use SSH (on port 2220) to 
# log into that level and continue the game.

solve:
once login bandit0,then 'ls' (list) there are a file name readme.To opened it using cat
and then there are password it called :ZjLjTmM6FvvyRnrb2rfNWOZOTa6ip5If
then, try ssh bandit1@bandit.labs.overthewire.org -p 2220
