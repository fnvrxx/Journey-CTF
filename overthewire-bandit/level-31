# There is a git repository at ssh://bandit30-git@localhost/home/bandit30-git/repo via the port 2220. The password for the user bandit30-git is the same as for the user bandit30.
# Clone the repository and find the password for the next level.
solve:
- same as level 30
- to solve in this level we need to know understand "git tag"
- because we know when we using command "cat README.md" 
- but,afterwards we know git tag there a called "secret"
- to opened using command "git show secret"
- and voila, you get the next password "fb5S2xb7bRyFmAvQYQGEqsbhVyJqhnDy"
