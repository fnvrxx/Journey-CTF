# There is a git repository at 
# ssh://bandit27-git@localhost/home/bandit27-git/repo via the port 2220. The 
# password for the user bandit27-git is the same as for the user bandit27.

# Clone the repository and find the password for the next level.
solve: 
- firstly, wee need to login in bandit27 using command "ssh"
- afterwards, using command "mktemp -d" to know location /tmp/ to access 
make a file
- using command git clone 
"ssh://bandit27-git@localhost:2220/home/bandit27-git/repo" DONT FORGET THE 
PORT
- inside that git, there a file called a README using command to know what 
inside "cat"
- voila "The password to the next level is: 
Yz9IpL0sBcCeuG7m9uQFt8ZNpS4HZRcN"
